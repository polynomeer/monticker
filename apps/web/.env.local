NEXT_PUBLIC_API_URL=http://localhost:8080
# 토스페이먼츠 공식 샘플 저장소(tosspayments/tosspayments-sample)에 공개된 테스트용 클라이언트
# 키다 — 시크릿 키가 아니라 브라우저에 노출돼도 안전한 공개 키이며, 실제 결제는 발생하지
# 않는다. 프로덕션에서는 반드시 실제 발급받은 라이브 클라이언트 키로 교체할 것
# (docs/deployment.md §2-5 참고).
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_D5GePWvyJnrK0W0k6q8gLzN97Eoq
