apiVersion: 1

providers:
  - name: monticker
    orgId: 1
    folder: monticker
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
